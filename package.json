{
  "name": "recruitsmart-crm-root",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix frontend",
    "build:frontend": "npm run build --prefix frontend",
    "build": "npm run build:frontend",
    "start": "npm start --prefix frontend"
  }
}
